  Implementation Plan: Template-Aware Document Generation

Architecture

1. Add thong-tu (Circular) as 6th document type — matches your reference document structure
2. Rich template structures — replace vague ["tiêu_đề", "căn_cứ", "nội_dung"...] with explicit HeaderTable → Căn_cứ → Ban_hành → Chương → Mục → Tiêu_chuẩn → Tiêu_chí → Nơi_nhận → Signature hierarchy
3. Structured field extraction — parse user prompt for (sender, receiver, dates, number, place) via LLM + regex fallback, present editable form in frontend
4. Few-shot examples — embed the parsed reference .docx as a golden example in system prompts
5. Exact structure matching — generated output follows the reference document's table-based header, article hierarchy, and section organization

Files to Modify

┌─────┬──────────────────────────────────────────┬───────────────────────────────────────────────────────────────────────────────────────┐
│  #  │                   File                   │                                        Change                                         │
├─────┼──────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────┤
│ 1   │ backend/src/services/template_service.ts │ Add thong-tu, rich structural definitions, field schemas, few-shot examples           │
├─────┼──────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────┤
│ 2   │ backend/src/services/orchestrator.ts     │ Enhanced prompts with field placeholders, structured output format, example injection │
├─────┼──────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────┤
│ 3   │ backend/src/middleware/validation.ts     │ Add thong-tu to DOCUMENT_TYPE_IDS                                                     │
├─────┼──────────────────────────────────────────┼──────────────────────────────────────────────────────────┤
│ 4   │ backend/src/routes/workflow.ts           │ New /fields endpoint for field extraction                                             │
├─────┼──────────────────────────────────────────┼──────────────────────────────────────────────────────────┤
│ 5   │ template-service/config.py               │ Add ThongTuVariables schema                                                           │
├─────┼──────────────────────────────────────────┼──────────────────────────────────────────────────────────┤
│ 6   │ template-service/generate_templates.py   │ Add thong-tu.docx template builder                                                    │
├─────┼──────────────────────────────────────────┼──────────────────────────────────────────────────────────┤
│ 7   │ frontend/app/generate/page.tsx           │ Field input form, few-shot preview                                                    │
├─────┼──────────────────────────────────────────┼──────────────────────────────────────────────────────────┤
│ 8   │ frontend/lib/api.ts                      │ New API calls for field extraction                                                    │
└─────┴──────────────────────────────────────────┴──────────────────────────────────────────────────────────┘

Prompt Strategy for Gemma 4 31B QAT

Given Gemma 4's 31B parameters and QAT optimization, it handlel but needs:
- Explicit XML/JSON delimiters in prompts (not just natural language)
- Clear role definition with "You are a Vietnamese government
- Few-shot with exact output format (not just "follow the template")

---
Do you approve this plan? Any adjustments before I begin imple

✻ Baked for 1m 45s

❯ tell me more about Given Gemma 4's 31B parameters and QAT optied instructions well but needs:
  - Explicit XML/JSON delimiters in prompts (not just natural language)
  - Clear role definition with "You are a Vietnamese government
  - Few-shot with exact output format (not just "follow the template"), how can you do that?

 Good question. Let me explain the specific prompt engineering techniques for Gemma 4 31B QAT and how I'll implement them:

1. Explicit XML/JSON Delimiters

Instead of: "Generate a document with header, legal basis, and articles" (vague, Gemma 4 may miss structure)

Use:
<document>
  <header_table>
    <agency>{{agency_name}}</agency>
    <doc_number>{{document_number}}</doc_number>
    <date>{{date_vn}}</date>
    <place>{{place}}</place>
  </header_table>

  <legal_basis>
    Căn cứ {{legal_citation_1}};
    Căn cứ {{legal_citation_2}};
    Theo đề nghị của {{proposer}};
  </legal_basis>

  <issuing_authority>{{issuer_name}}</issuing_authority>

  <articles>
    {% for article in articles %}
    <article number="{{article.number}}">
      <title>{{article.title}}</title>
      <content>{{article.content}}</content>
    </article>
    {% endfor %}
  </articles>

  <distribution_list>
    <recipients>{{recipients}}</recipients>
  </distribution_list>

  <signature>
    <role>{{signatory_role}}</role>
    <name>{{signatory_name}}</name>
  </signature>
</document>

Why this works for Gemma 4:
- XML tags act as anchors — the model was trained on code and riggers pattern-matching behavior
- Self-closing delimiters prevent "hallucination drift" — once </article> appears, the model knows that section is done
- Gemma 4's QAT (Quantization Aware Training) preserves these an free-form text

2. Clear Role Definition with "Act As"

Instead of: "You are a professional Vietnamese government docu

Use:
You are a senior drafter at the Vietnamese Ministry of Education and Training (Bộ Giáo dục và Đào tạo). Your job is to draft official administrative
documents (Thông tư, Quyết định, Chỉ thị, etc.) that are EXACT020/NĐ-CP.

You MUST follow this STRUCTURAL CONTRACT:
1. Every document starts with a 2-column header table (agency left, national emblem right)
2. Legal basis ("Căn cứ") paragraphs are numbered and cite exa
3. Articles are numbered "Điều X." with bold titles
4. Chapters ("Chương"), Sections ("Mục"), Standards ("Tiêu chuse the exact hierarchy from the example
5. Distribution list ("Nơi nhận") is a bullet list
6. Signature block uses "KT. [POSITION]" / "[ROLE]" / "(Đã ký)

OUTPUT FORMAT: You must output ONLY the document content. Do N code blocks, or commentary. Start with the header, end withthe signature.

Why this works:
- Specific persona (senior drafter at MOET) grounds the model
- "STRUCTURAL CONTRACT" and "MUST" create emphasis that Gemma 4 respects
- Numbered rules are easier for the model to check against tha
- "OUTPUT FORMAT" constraint prevents the model from adding "Here's your document:" preamble

3. Few-Shot with Exact Output Format

Instead of: "Follow this template" (the model has to infer what you mean)

Use:
Here is an EXAMPLE of a correctly formatted Thông tư (Circular

=== EXAMPLE START ===
[Insert the first 500-1000 words of parsed reference .docx here, with Jinja2 placeholders]

Key structural rules from this example:
- Header: 2-column table, agency left, national emblem + date
- Legal basis: Each "Căn cứ" on its own line, ending with semicolon
- Issuing line: "Bộ trưởng Bộ [NAME] ban hành Thông tư..." (no
- Articles: "Điều 1." (bold), content follows immediately
- Chapter/Section hierarchy: "Chương I" → "Mục 1" → "Tiêu chuẩ [description]"
- Each criterion has numbered sub-items (1., 2., 3.)
- Distribution: 2-column table with recipients left, signature
- Signature: "KT. [POSITION]" / "[ROLE]" / blank lines / "(Đã ký)" / "[NAME]"

=== EXAMPLE END ===

Now, generate a new document with the SAME structure but DIFFERENT content based on the user's request.
User request: {{user_prompt}}
Fields to fill: {{extracted_fields}}

Why this works for Gemma 4:
- The "EXAMPLE START/END" delimiters clearly separate the refe
- Inline annotations ("Key structural rules") guide the model without adding length
- Gemma 4's 128K context can handle 500-1000 word examples + t
- The explicit "SAME structure but DIFFERENT content" instruction prevents the model from copying the example text

Implementation in Code

Here's how I'll implement this in orchestrator.ts:

// New: Build few-shot prompt with parsed reference document
function buildFewShotPrompt(docType: string, userPrompt: strining>): string {
  const example = getFewShotExample(docType); // Returns parsed .docx content as string
  const fieldInstructions = Object.entries(fields)
    .map(([key, value]) => `${key}: "${value}"`)
    .join('\n');

  return `<role>
You are a senior drafter at the Vietnamese Ministry of Education and Training.
You draft official documents exactly per Decree 30/2020/NĐ-CP.
</role>

<structural_contract>
1. Header table: 2 columns (agency left, emblem+date right)
2. Legal basis: "Căn cứ [Law] ngày [date];" each on own line
3. Issuing authority line before "Điều 1"
4. Articles: "Điều X." bold, content follows
5. Hierarchy: Chương > Mục > Tiêu chuẩn > Tiêu chí (with numbe
6. Distribution table: recipients left, signature right
7. Signature: "KT." / role / "(Đã ký)" / name
</structural_contract>

<example>
${example}
</example>

<fields>
${fieldInstructions}
</fields>

<instruction>
Generate a ${docType} document matching the EXACT structure of
Use the provided fields. Output ONLY the document content. No explanations.
</instruction>

<user_request>
${userPrompt}
</user_request>`;
}

Frontend Field Form

For the "form + LLM extraction" dual approach:
  // Extract fields from user prompt using a lightweight LLM call
  async function extractFields(prompt: string, docType: string): Promise<Record<string, string>> {
    const extractionPrompt = `
  Extract these fields from the user's request. Return as JSON.
  If a field is missing, return empty string.

  Fields: sender, receiver, date, document_number, place, subject

  User request: "${prompt}"
  `;
    // Call LM Studio with extraction prompt
    // Parse JSON response
    // Return fields
  }

  Then in the frontend:
  1. User types natural language prompt
  2. Backend extracts fields automatically
  3. Frontend shows editable form with extracted values
  4. User can correct fields before generation
  5. Corrected fields are sent with the generation request

  ---
  Does this level of detail answer your question? Should I proceed with implementation?

✻ Sautéed for 1m 45s

❯ /export 12626conversation
  ⎿  Failed to export conversation: EEXIST: file already exists, mkdir 'C:\Users\PC'

  7 tasks (4 done, 3 open)
  ◻ Implement template-aware document generation
  ◻ Quality review and testing
  ◻ Summary and documentation
  
  