# conversion-service
.venv/
__pycache__/
*.pyc
.pytest_cache/
work/
eval/out/
# eval/fixtures/media/*.png stays tracked: verify_p0a.py (release gate) renders
# the seal fixtures from these committed files — ignoring them broke CI with
# "0 anchors vs 1 seals". They are deterministic outputs of _make_seals.py.
